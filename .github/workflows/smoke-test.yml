name: Smoke Test

on:
  workflow_dispatch

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Echo smoke test phrase
        run: echo 'actions smoke test ok'
